#include <stdio.h>
/**
 * main - the entry point of a program that allows puts function to print
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
